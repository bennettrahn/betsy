<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title><%= content_for?(:title) ? yield(:title) : "3Bae" %></title>

  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body>
  <header>
    <%= link_to "Home", root_path %>
  </header>

  <header class="page-header row">
    <h2 class="columns small-12">
      <%= link_to "3Bae", root_path %>
      <small>Good Things Happen in Threes</small>
    </h2>

    <nav class="columns small-12">
      <%= link_to "View all products", products_path, class: "button" %>

      <%# unless session[:merchant_id] == nil %>
      <%= link_to "All merchants", merchants_path, class: "button" %>
      <%# add a dropdown menu here %>
      <%# end %>

      <!-- <%= link_to "Create new category", new_category_path, class: "button "%> -->
      <%= link_to "All categories", categories_path, class: "button" %>
      <% if session[:merchant_id] %>
        <%= link_to "Create new category", new_category_path, class: "button "%>
      <% end %>

      <%# add a dropdown menu here to "Shop by categories" %>

      <div class="float-right">
        <%= link_to session[:cart] ? order_path(session[:cart]) : products_path do %>
        <%= image_tag 'cart.png', size: "40x40" %>
        <% end %>
        <!-- <a href="/products"><img src="/images/cart.png" alt="cart"></a> -->
        <!-- <%= link_to "My Cart", products_path, class: "button" %> -->
        <% if session[:merchant_id] %>
        <%= link_to "Log out", logout_path, class: "button" %>
        <% else %>
        <%= link_to "Log in", '/auth/github', class: "button" %>
        <% end %>
      </div>
    </nav>
  </header>

  <% if flash[:message] %>
  <section class="row status <%= flash[:status] %>">
    <%= flash[:message] %>
    <% if flash[:details] %>
    <ul>
      <% flash[:details].each do |name, problems| %>
      <% problems.each do |problem| %>
      <li><%= name %>: <%= problem %></li>
      <% end %>
      <% end %>
    </ul>
    <% end %>
  </section>
  <% end %>

  <% if flash[:result_text] or flash[:messages] %>
  <section class="row status <%= flash[:status] %>">
    <h3><%= flash[:status] == :failure ? "A problem occurred: " : "" %><%= flash[:result_text] %></h3>
    <% if flash[:messages] %>
    <ul>
      <% flash[:messages].each do |name, problems| %>
      <% problems.each do |problem| %>
      <li><%= name %>: <%= problem %></li>
      <% end %>
      <% end %>
    </ul>
    <% end %>
  </section>
  <% end %>

  <%= yield %>

</body>
</html>
